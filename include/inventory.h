#include "item.h"

#define ARMOUR_MAX 12       /* Armour done in 3s, 4 sets max */
#define WEAPON_MAX 8        /* Seems fair enough imo         */
#define ITEM_MAX 12         /* Woo, basic items!             */
#define INVENTORY_MAX 32    /* Sum of the other categories   */

typedef struct Inventory Inventory;

struct Inventory {

};